#include "picodet.h"

/*

int main() {
	PicoDet picodet("weights/picodet/picodet_m_416_coco.onnx", "weights/picodet/coco.names", 0.5, 0.5);
	string imgpath = "data/person.jpg";
	Mat img = imread(imgpath);

	picodet.detect(img);

	static const string WinName = "PicoDet in ONNXRuntime";
	namedWindow(WinName, WINDOW_AUTOSIZE);
	imshow(WinName, img);
	waitKey(0);
	destroyAllWindows();
}


*/
